#include <iostream>
using namespace std;
class Node
{
    Node *next;
    int data;
    Node(int data)
    {
        this.data = data;
    }
};
Node *head = nullptr;
void insertAtend(int d)
{
    if (node->next == nullptr)
    {
        cout << "Overflow.";
        return;
    }
    Node *next = new Node(d);
}
void display()
{
}